## For Non-Admin users, which is common employees in a Corporate Device.

### This command will allow running Powershell Script without restriction 

`set-executionpolicy remotesigned -Scope CurrentUser`